# Benchmarks

Formal benchmarks have not been run yet. This file will be updated with real, reproducible benchmark results once benchmark tooling is in place.

## Planned

- Job enqueue throughput (via `go test -bench`)
- Job dequeue latency under varying concurrency
- End-to-end scheduling latency
- Memory usage under load

Contributions welcome -- see CONTRIBUTING.md.
